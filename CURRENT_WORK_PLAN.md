# Актуальный план работ VIKS

Дата сверки: 30.08.2026.

## Релиз 2.21.3 — Роман и исторический состав (31 августа)

- Подтверждены удаление/повторное создание сотрудников 91/92/93 → 103/105/104 → 106/107/108, пустой roster УАЗ361 и ложная неполнота при 63 часах.
- Добавлены историческое чтение состава/ФИО, сохранение снимка перед удалением участника, явные алиасы только внутри конкретной заявки и защита черновика от сложения повторов.
- Подготовлен hash-locked скрипт `repair_smr_roman_rosters.py`: только 10 неучтённых заявок Романа; 5 ранее согласованных секций Персонала получают «не работала» по явному подтверждению владельца. Часы, работы, цены, статусы заявок и учёт не переписываются.
- На копии данных обе заявки 1261/1278 успешно завершены с 63 часами, прежними работами и суммами. Прямое сравнение с бэкапом подтверждает неизменность всех фактических строк при восстановлении состава.
- Выложено: commit `ea0d69a`, версия 2.21.3; без уведомления в беседу. 70 backend-тестов, 7 JS-проверок черновика, lint и сборка пройдены.
- На сервере восстановлены 10 неучтённых составов Романа и 5 согласованных секций Персонала. Бэкап `data/backups/pre-smr-roster-repair-20260831-162952.db`, hash плана `5588308ec3eb25cd98e0725aeeb0dae2fec4c295674abf92ff88be1dd8eb89e5`; повторный план пуст, 15 записей аудита.
- С бэкапом побайтно по значениям строк совпадают 15 заявок, 63 строки часов, 6 плановых и 51 дополнительная работа. Учёт сохранён; отчёты автоматически не завершались.
- Live API: 1261/1278 имеют 63 часа и is_complete=true; 1325/1343/1345 показывают 7 участников, но фактических часов пока нет. 1019 также требует реальных недостающих часов, не подставлялись нули. Все 5 отчётов Персонала полны и учтены.
- Проверено 15 отчётов через API, nginx -t, healthy API/MAX, публичные HTML/health 200 и версия 2.21.3. Новых ERROR/Traceback/OperationalError/500 после запуска не обнаружено. Проверки личного черновика/экрана телефона Романа нет: нужен его повторный вход в отчёт без очистки данных сайта.

## Релиз 2.21.2 — полное редактирование СМР (31 августа 2026)

- Исправлена причина `no such savepoint`: записи СМР используют отдельное соединение, чужой запрос не может закоммитить их транзакцию.
- Готовый отчёт редактируется целиком, по стабильным ID строк: основная часть и дополнения видны вместе, сохранённые цены не меняются автоматически.
- Добавлены версия снимка и идемпотентность сохранения/дополнений. Старый неполный редактор получает 409 до изменения данных.
- Заполненность учитывает дополнительные часы. Нулевые часы видны в общей выгрузке, просмотре и новом редакторе.
- Подготовлен `scripts/repair_smr_reentered_hours.py`: dry-run, проверка хеша плана, резервная копия, точечное удаление только доказанных дублей, атомарный аудит. Учтённое/архив не затрагивает.
- Выложено 31 августа: commit `b9f554e`, версия 2.21.2, без уведомлений в MAX. 123 теста, lint и сборка прошли.
- В рабочей БД удалены 40 подтверждённых повторных строк в 10 заявках; повторный dry-run пустой. Бэкап: `data/backups/pre-smr-reentry-repair-20260831-110748.db`, по каждой заявке создан аудит до/после. Работы, расценки и исходные дополнения сохранены.
- На сервере сверены 12 отчётов (редактор/сводка), повторное no-op сохранение 1122 и 1211, все 6 XLSX заявки 1211: 72 чел.-ч, 13 работ. Заявка 1122 учтена, её данные не менялись.
- Визуальный браузер сейчас недоступен; не считать мобильное визуальное тестирование выполненным.

## Релиз 2.21.1 — варианты файлов СМР

- По команде владельца добавлены три динамические выгрузки для сравнения Кадрами: общий новый формат, по бригадам, единый состав заявки. Привычные файлы сохранены.
- Новые варианты доступны офису и прорабу своей заявки; финансовые поля ограничены ролью. Они не изменяют данные и не выполняют перевод сотрудников или автоматическое деление исторических общих работ.
- Нулевые сохранённые часы видны; источники объединённых заявок сохраняются; нераспределённые работы учитываются в новом бригадном файле один раз.
- Владелец явно запросил деплой и сообщения в беседу расстановок для этого релиза. Использовать единственный запуск `scripts/deploy_release.sh --notify`, без дополнительной ручной отправки тех же событий.
- Отдельно остаются невыполненными согласованные изменения полного цикла: стабильные карточки/перемещения участников, обязательность часов и работ, управляемые доли совместных работ, единый выбор услуг и адресный возврат реально неполных неучтённых неархивных СМР. Добавление вариантов файлов не означает завершение этих задач.

Документ объединяет задачи из текущей переписки, историю Git, автоматические проверки и read-only проверку рабочей базы сервера. Он нужен, чтобы следующие изменения не дублировали уже выполненную работу.

## Выполнено и подтверждено

- Уведомление прорабу со всеми изменениями заявки, внесёнными модератором.
- Отметка «Учтено» в готовых СМР, массовое действие, фильтр, явное выделение и отсутствие прокрутки вверх.
- Группировка СМР по прорабу, объекту и дате; глубокий поиск по содержимому отчётов.
- Исправлены обмен техникой, хронология объектов и будущие даты отпусков/больничных.
- Добавлены настраиваемые события уведомлений по ролям, центр уведомлений с подробным просмотром и системный контроль сбоев.
- Telegram полностью удалён из рабочего приложения; основной канал — MAX.
- PWA перенесено на новый домен, старые ссылки заменены, добавлены стабильный вход через MAX и Passkey с инструкцией и запасным входом по коду.
- Добавлены роли «Кадры» и «Сотрудник», ролевые главные страницы, управление банами, паролями ролей и подтверждение их замены.
- Исправлена смена роли. Марченко Виталий Анатольевич сейчас имеет роль `moderator`.
- В рабочей базе нет активных пользователей без роли и нет пользователей с установленным баном.
- Кочетов Андрей Владимирович и Чулков Максим Юрьевич — импортированные записи водителей; обе записи архивированы (`driver`, `is_deleted=1`), а не заблокированы.
- Архивирование СМР до 31.07 выполнено: в журнале сервера зафиксировано 827 архивированных отчётов, архив исключён из должников.
- Добавлены короткие информативные номера заявок с русской датой и русские имена выгружаемых файлов.
- Исправлены нижнее меню, центральная кнопка создания, набор вкладок по ролям, закрепление панели снизу и safe-area мобильных модалок.
- Добавлены статистика ресурсов, учёт частичных составов и нескольких объектов, глубокий поиск, общий пароль водителей и проверка привязки MAX.
- Добавлены список проблем на главной, одноразовое ролевое обучение, управление автопубликацией и паролями ролей.
- Прораб может вводить отдельную ЗП участников СМР; общие часы необязательны; значение `0` допустимо.
- Готовый СМР можно редактировать или полностью очищать после подтверждения.
- Импорт справочника СМР использует D/E/G/H и игнорирует служебные расчётные столбцы.
- На главной прораба есть массовое освобождение бригад и техники с фактическим временем.
- Прораб и офис могут создать помеченную заявку строго за вчера только для СМР, без публикации.
- Формат уведомлений об обновлении и защита от двойной отправки закреплены в релизных скриптах. Отправка выполняется только по явному указанию пользователя.

## Текущая версия 2.20.4

- В объединённом СМР сотрудники, часы, ЗП, основные и дополнительные работы разделены по исходным заявкам и объектам.
- Одинаковая бригада, сотрудник или работа могут находиться на нескольких объектах с независимыми значениями.
- Отправка, повторное открытие, редактирование готового отчёта и допотчёт сохраняют строки в соответствующей исходной заявке.
- Excel, хронология и подробности отчёта используют ту же объектную привязку и не соединяют названия через слеш.
- Старые строки восстанавливаются по составу заявки и плану объекта, если источник определяется однозначно.
- Массовое освобождение на главной теперь работает одним нажатием: после выбора ресурсов сразу доступна кнопка «Освободить», отдельный подтверждающий ввод удалён.
- Проверки: 77 backend-тестов пройдены; production-сборка frontend успешна.

- Для каждой бригады СМР формируется собственный XLSX; ZIP больше не используется.
- Кнопка файла открывает карточку отчёта с общей информацией, отдельными файлами и скачиванием всех файлов.
- Для Кадров и бухгалтерии неполные отчёты скрыты по умолчанию и не могут быть отмечены учтёнными.
- Освобождённые бригады и техника корректно исключаются из занятости и доступны для новой заявки.
- Проблемы на главной сгруппированы по типам; накопленные замечания frontend-lint устранены.
- Проверки: 80 backend-тестов пройдены; линтер и production-сборка frontend успешны.

- Исправлен аварийный экран запуска PWA после релиза 2.20.2: восстановлены обязательные импорты анимаций.
- Добавлен регрессионный тест на целостность импортов `framer-motion`.
- Проверки: 81 тест, линтер, production-сборка и запуск на мобильном размере экрана успешны.

- Блок «Требует внимания» фильтруется сервером по реальным полномочиям каждой роли.
- Прораб получает только собственные задачи СМР и проблемы своих бригад, включая непривязанных сотрудников и отсутствие бригадира.
- Бригадир видит непривязанных рабочих своих бригад и может создать для них ссылку-приглашение.
- Кадры видят доступные им задачи по СМР, бригадам и водителям; полевые роли не получают чужие административные проблемы.
- Проверки: 81 тест, линтер и production-сборка frontend успешны.

Версия подготовлена к релизу с push, деплоем и сообщениями в MAX по явной команде пользователя.

## Оставшиеся задачи

### 1. Приёмка объединённых СМР

- После разрешения на деплой проверить реальные группы заявок `1103/1104`, `1122/1123`, `1141/1142` и историческую `235/236`.
- Проверить общий режим и режим «По бригадам», повторное редактирование, допотчёт, Excel и хронологию.
- Исторические произвольные допработы, которые раньше были записаны только в первую заявку, нельзя безопасно распределить автоматически: их объект нужно один раз указать при редактировании готового отчёта.

### 2. Полная финансовая регрессия СМР

- Покрыть тестами цены СМР, расценки ЗП, отдельную ЗП участников, нулевые и индивидуальные часы.
- Проверить плановые и дополнительные работы, общий и бригадный режимы, частичные составы, допотчёты, редактирование и полную очистку.
- Проверить одинаковые позиции КП на разных объектах и неизменность исторических цен после обновления справочника.
- Сверить суммы интерфейса, Excel, статистики объекта и финансового аудита на одном наборе контрольных данных.

### 3. Итоговый аудит уведомлений

- Статически подтверждено: все явно заданные ключи уведомлений зарегистрированы в каталоге настроек.
- Нужен сквозной тест по ролям и каналам: MAX в личные сообщения, группа только для расстановки/явно разрешённых релизов, Push и центр уведомлений.
- Проверить тексты, ссылки из колокола, отсутствие дублей и отключение каждого события в настройках.

### 4. Финальный дизайн-аудит

- Выполнять после функциональной стабилизации, как было согласовано.
- Проверить все роли и основные экраны на iPhone/Safari PWA, Android/Chrome и ПК.
- Проверить safe-area, высоту и прокрутку модалок, нижнюю панель, центральную кнопку, длинные названия, таблицы, поиск и массовые действия.
- Отдельно устранить накопленный технический долг frontend-lint; текущие ошибки существовали до версии 2.20.1 и не мешают production-сборке, но затрудняют дальнейшие изменения.

### 5. Технические улучшения после функциональных задач

- Сохранить исходного автора и время каждой строки СМР при повторной модераторской записи, вместо перезаписи автора всей заменяемой секции (`D12`).
- Разделить крупные компоненты СМР и административные страницы, добавить компонентные тесты для мобильных состояний.
- После будущего деплоя проверить health-check, watchdog, планировщик, доставку MAX/Push и отсутствие бесконечного режима обновления.

## Порядок дальнейшей работы

1. Проверить релиз 2.20.2 на реальных данных после деплоя.
2. Приёмка объединённых СМР на реальных данных.
3. Полная финансовая регрессия СМР.
4. Сквозная проверка уведомлений по ролям и каналам.
5. Итоговый дизайн-аудит и мобильная полировка.
6. Технический рефакторинг и закрытие `D12`.

## Точечное исправление З-030826-01 — 31.08.2026

- По явному подтверждению владельца добавлены по 9 часов Тушову Игорю (91), Шапкину Алексею (92), Борисову Илье (93) в заявку 1019 за 04.08.2026, историческая бригада 27.
- Сервер: 7 участников, 63 человеко-часа, отчёт полный. Работы, расценки, имеющиеся часы и статус учёта не изменены. API просмотра, полного редактора и карточки файлов согласованы.
- Аудит `smr_hours_corrected`, запись 731; резервная копия `/root/viksstroy_bot/data/backups/pre-1019-confirmed-hours-20260831-171400-236725.db`. Нового деплоя и сообщений в MAX не было.
- Исторический код `c7a90d8` сразу утверждал СМР прораба без проверки полноты. Уже первое сохранение 05.08 содержало только 4 строки/36 часов. Журналы не позволяют определить, были ли отсутствующие часы введены, но потеряны формой; обвинять пользователя в незаполнении оснований нет.

## Версия 2.22.0 — большой журнал аудита

- Подготовлен новый журнал `product_audit_events`: действия, переходы, формы, ошибки клиента/сервера, медленные запросы, повторные клики, мобильное переполнение модалок и показатели производительности.
- Значения полей и тела запросов не пишутся; секретные ключи маскируются на клиенте и сервере. Хранение по умолчанию 180 дней, настройка 30–730 дней.
- Только Супер-Админ получает сводку за 2/7/30 дней, поиск, подробности, автоматические кандидаты на улучшение и CSV. Есть read-only скрипт `scripts/analyze_product_audit.py` для совместного периодического разбора.
- Локальные проверки: существующие и новые backend-тесты, клиентские privacy-проверки, ESLint и production-сборка проходят. Деплой ещё не выполнялся, уведомление в MAX не отправлялось.

### Полный отчёт СМР и нормализация сотрудников

- Отчёт за произвольный период перестроен на три листа: «Сводка», «По заявкам» и «Работы». В нём отдельно показаны введённая ЗП участника и расчётная ЗП по расценкам работ.
- Архивные СМР входят всегда и отбираются по дате выполнения работ. По умолчанию включены только учтённые отчёты; завершённые неучтённые добавляются отдельной галочкой.
- Добавлен устойчивый реестр личностей сотрудников поверх старых и удалённых карточек бригад. Исторические часы не переписываются; сомнительные совпадения объединяются только вручную Супер-Администратором с подтверждением.
- Для Супер-Администратора добавлена панель аудита сотрудников: неполные ФИО, нераспознанные исторические ID, кандидаты в дубли, поиск и безопасное исправление канонического ФИО.
- На вкладке «Готовые» добавлена массовая архивация. В смешанном выборе показывается предупреждение, а архивируются только учтённые отчёты. Все части объединённого СМР архивируются и восстанавливаются атомарно.
- Проверка на копии рабочей базы: 87 исторических идентификаторов сохранены, архивные строки попадают в августовский отчёт. Полный backend-набор: 135 тестов пройдено. Деплой ещё не выполнялся.
